applicative-order: evaluate arguments before passing on to other expressions. 
=> (p) parameter is evaluated infitely

normal-order: expand everything to primitve form before evaluating. hence as condition is not false, (p) is never evaluated